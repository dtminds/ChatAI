{
  "name": "@chatai/backend",
  "private": true,
  "license": "UNLICENSED",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "pnpm --filter @chatai/contracts build && tsc -p tsconfig.json",
    "db:codegen": "node scripts/codegen-db-schema.mjs",
    "dev": "pnpm --filter @chatai/contracts build && NODE_ENV=development tsx watch src/server.ts",
    "start": "node dist/server.js",
    "worker": "node dist/worker.js",
    "worker:dev": "pnpm --filter @chatai/contracts build && NODE_ENV=development tsx watch src/worker.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm --filter @chatai/contracts build && tsc --noEmit"
  },
  "dependencies": {
    "@chatai/contracts": "workspace:*",
    "@fastify/cookie": "^11.0.2",
    "@fastify/env": "^7.0.0",
    "@fastify/jwt": "^10.0.0",
    "@sinclair/typebox": "^0.34.41",
    "altcha-lib": "^2.0.3",
    "argon2": "^0.45.0",
    "fastify": "^5.6.2",
    "fastify-plugin": "^6.0.0",
    "ioredis": "^5.11.1",
    "kysely": "^0.29.0",
    "mysql2": "^3.15.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "dotenv": "^17.4.2",
    "kysely-codegen": "^0.20.0",
    "tsx": "^4.21.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.5"
  }
}
